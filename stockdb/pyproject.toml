[project]
name = "StockSense-Stockdb"
version = "1.5.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "agno>=2.6.20",
    "alive-progress>=3.3.0",
    "fastapi>=0.135.3",
    "orjson>=3.11.3",
    "polars[cloudpickle,deltalake,pyarrow]>=1.38.1",
    "pydantic-settings>=2.10.1",
    "pyyaml>=6.0.3",
    "rich>=14.3.1",
    "scalar-fastapi>=1.6.1",
    "scipy>=1.16.2",
    "stocksense-core[data]",
    "uvicorn>=0.41.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "ipykernel>=6.30.1",
    "jupyterlab>=4.5.6",
    "pytest>=8.4.2",
    "pytest-asyncio>=1.2.0",
    "pytest-cov>=7.1.0",
]

[build-system]
requires = ["uv_build>=0.8.18,<0.9.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "api"
module-root = ""

[tool.uv.sources]
stocksense-core = { git = "https://github.com/Akashdesarda/stock-research-platform.git", subdirectory = "core" }

[tool.pyrefly]
project-includes = ["**/*.py*", "**/*.ipynb"]
